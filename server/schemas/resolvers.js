const { Query } = require('mongoose');
const { UploadImage } = require('../models');

const resolvers = {
    Query: {
        // query: async () => {}
    },

    Mutation: {
        // mutation: async () => {}
    }
};

module.exports = resolvers;